[project]
name = "webspan"
version = "0.1.1"
description = "Bridge HTML and Python in Pythonista to build simple apps"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "toga>=0.5.6",
]
